const cardsFilter = document.querySelector('[data-js=filter]');
const filterButton = document.querySelector('[data-js=filter-button]');

filterButton.addEventListener('click', () => {
if (
    card.house.value === 'gryffindor',

})

